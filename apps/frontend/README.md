# Frontend

This folder hosts the web UI. The initial focus is the setup and login flow, which sets the visual language for the dashboard.

## Development
Open `index.html` directly in a browser while the frontend stack is still being wired.

## Structure
- `index.html` — setup screen layout
- `styles.css` — design system and layout
- `app.js` — simple UI behavior (help popups)
